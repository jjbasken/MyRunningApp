<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws the real UI; this theme only styles the launch window
         before the first frame. -->
    <style name="Theme.MyRunningApp" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/ic_launcher_background</item>
    </style>
</resources>
